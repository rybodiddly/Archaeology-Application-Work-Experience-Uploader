The dump folder stores incremental uploads for each upload event for the purposes of debugging.
Should an upload fail, just open the resulting dump html files in a browser to see which ones
contain errors. This is useful when you've missed an additional space at the end of a pif or
borden number.
